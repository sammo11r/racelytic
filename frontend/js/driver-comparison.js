let comparisonData = null;

function classified(position) { const value=Number(position); return Number.isFinite(value)&&value>0; }
function duelScore(races, firstKey, secondKey) { return races.reduce((score,race)=>{ if(!classified(race[firstKey])||!classified(race[secondKey])||Number(race[firstKey])===Number(race[secondKey]))return score; Number(race[firstKey])<Number(race[secondKey])?score.first++:score.second++; return score; },{first:0,second:0}); }
function metric(label, first, second) { const a=Number(first||0),b=Number(second||0); return `<div class="comparison-metric"><strong class="${a>b?'leader':''}">${fmtNumber(first)}</strong><span>${esc(label)}</span><strong class="${b>a?'leader':''}">${fmtNumber(second)}</strong></div>`; }

function renderOverview() {
  const [first,second]=comparisonData.drivers;
  return `<div class="comparison-scorecard">${metric('World championships',first.totalChampionshipWins,second.totalChampionshipWins)}${metric('Race wins',first.totalRaceWins,second.totalRaceWins)}${metric('Podiums',first.totalPodiums,second.totalPodiums)}${metric('Pole positions',first.totalPolePositions,second.totalPolePositions)}${metric('Fastest laps',first.totalFastestLaps,second.totalFastestLaps)}${metric('Career points',first.totalPoints,second.totalPoints)}</div>`;
}

function renderTeammates() {
  const races=comparisonData.sharedRaces,[first,second]=comparisonData.drivers;
  if(!races.length)return '<div class="empty-state">These drivers were never teammates in the available race results.</div>';
  const raceScore=duelScore(races,'firstPosition','secondPosition'),qualifyingScore=duelScore(races,'firstQualifying','secondQualifying');
  const seasons=[...new Set(races.map(race=>race.year))].sort((a,b)=>b-a);
  return `<div class="teammate-summary"><div><span>Shared races</span><strong>${fmtNumber(races.length)}</strong></div><div><span>Race head-to-head</span><strong>${raceScore.first}–${raceScore.second}</strong><small>${esc(first.name)} – ${esc(second.name)}</small></div><div><span>Qualifying head-to-head</span><strong>${qualifyingScore.first}–${qualifyingScore.second}</strong><small>Classified sessions</small></div></div><div class="table-wrap"><table><thead><tr><th>Season</th><th>Team</th><th>Races</th><th>Race H2H</th><th>Qualifying H2H</th><th>Points</th></tr></thead><tbody>${seasons.map(year=>{const season=races.filter(race=>race.year===year),race=duelScore(season,'firstPosition','secondPosition'),qualifying=duelScore(season,'firstQualifying','secondQualifying'),teams=[...new Set(season.map(item=>item.constructorName).filter(Boolean))].join(', '),firstPoints=season.reduce((sum,item)=>sum+Number(item.firstPoints||0),0),secondPoints=season.reduce((sum,item)=>sum+Number(item.secondPoints||0),0);return `<tr><td><a href="/season.html?year=${year}">${year}</a></td><td>${esc(teams)}</td><td>${season.length}</td><td>${race.first}–${race.second}</td><td>${qualifying.first}–${qualifying.second}</td><td>${fmtNumber(firstPoints)}–${fmtNumber(secondPoints)}</td></tr>`;}).join('')}</tbody></table></div>`;
}

function renderSharedRaces() {
  const races=comparisonData.sharedRaces,[first,second]=comparisonData.drivers;
  if(!races.length)return '<div class="empty-state">No shared races found.</div>';
  return `<div class="table-wrap"><table><thead><tr><th>Race</th><th>Constructor</th><th>${esc(first.name)}</th><th>${esc(second.name)}</th><th>Qualifying</th></tr></thead><tbody>${races.map(race=>`<tr><td><a href="/race.html?id=${encodeURIComponent(race.raceId)}">${esc(race.year)} ${esc(race.officialName)}</a><small>Round ${esc(race.round)}</small></td><td>${esc(race.constructorName||'—')}</td><td>${esc(race.firstPositionText||race.firstPosition||'—')}</td><td>${esc(race.secondPositionText||race.secondPosition||'—')}</td><td>${race.firstQualifying||'—'}–${race.secondQualifying||'—'}</td></tr>`).join('')}</tbody></table></div>`;
}

function selectComparisonView(value) { document.querySelectorAll('[data-comparison-panel]').forEach(panel=>{panel.hidden=panel.dataset.comparisonPanel!==value;}); document.querySelectorAll('[data-comparison-view]').forEach(button=>{const active=button.dataset.comparisonView===value;button.classList.toggle('active',active);button.setAttribute('aria-selected',String(active));}); }
function renderComparison() { const [first,second]=comparisonData.drivers; document.getElementById('comparison-content').innerHTML=`<section class="comparison-driver-heads"><a href="/driver.html?id=${encodeURIComponent(first.id)}"><span>${esc(first.nationalityCountryId||'')}</span><h2>${esc(first.name)}</h2></a><div>HEAD TO HEAD</div><a href="/driver.html?id=${encodeURIComponent(second.id)}"><span>${esc(second.nationalityCountryId||'')}</span><h2>${esc(second.name)}</h2></a></section><section class="analysis-visualization-menu comparison-view-menu"><div class="analysis-visualization-heading"><div class="eyebrow">COMPARISON</div><strong>Choose a view</strong></div><div class="analysis-visualization-tabs comparison-tabs" role="tablist"><button type="button" class="active" role="tab" aria-selected="true" data-comparison-view="overview"><span>Career overview</span><small>All-time totals</small></button><button type="button" role="tab" aria-selected="false" data-comparison-view="teammates"><span>Teammate battle</span><small>Season by season</small></button><button type="button" role="tab" aria-selected="false" data-comparison-view="races"><span>Shared races</span><small>Race by race</small></button></div></section><div class="comparison-workspace"><section data-comparison-panel="overview">${renderOverview()}</section><section data-comparison-panel="teammates" hidden>${renderTeammates()}</section><section data-comparison-panel="races" hidden>${renderSharedRaces()}</section></div>`; document.querySelectorAll('[data-comparison-view]').forEach(button=>button.addEventListener('click',()=>selectComparisonView(button.dataset.comparisonView))); }

async function compareDrivers() { const first=document.getElementById('comparison-driver-one').value,second=document.getElementById('comparison-driver-two').value;if(first===second)return setError('comparison-content','Choose two different drivers.');document.getElementById('comparison-content').innerHTML='<div class="loading-state">Building comparison…</div>';try{comparisonData=await getJSON(`/api/drivers/compare?ids=${encodeURIComponent(first)},${encodeURIComponent(second)}`);renderComparison();}catch(error){setError('comparison-content',error.message);} }
getJSON('/api/drivers?limit=1000').then(drivers=>{const ordered=[...drivers].sort((a,b)=>Number(b.totalRaceWins)-Number(a.totalRaceWins)||a.name.localeCompare(b.name)),options=ordered.map(driver=>`<option value="${esc(driver.id)}">${esc(driver.name)}</option>`).join('');document.getElementById('comparison-driver-one').innerHTML=options;document.getElementById('comparison-driver-two').innerHTML=options;document.getElementById('comparison-driver-two').selectedIndex=Math.min(1,ordered.length-1);compareDrivers();}).catch(error=>setError('comparison-content',error.message));
document.getElementById('compare-drivers').addEventListener('click',compareDrivers);
