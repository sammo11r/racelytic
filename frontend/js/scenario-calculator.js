const SCENARIO_SYSTEMS={modern:{name:'Modern',race:[25,18,15,12,10,8,6,4,2,1],sprint:[8,7,6,5,4,3,2,1]},2003:{name:'2003–2009',race:[10,8,6,5,4,3,2,1],sprint:[]},1991:{name:'1991–2002',race:[10,6,4,3,2,1],sprint:[]},classic:{name:'Classic',race:[9,6,4,3,2,1],sprint:[]}};
let scenarioData=null,predictions=new Map();

function scenarioSystem(){return SCENARIO_SYSTEMS[document.getElementById('scenario-points').value];}
function awarded(position,scale){const index=Number(position)-1;return index>=0&&index<scale.length?Number(scale[index]):0;}
function futureScore(result,position,system){let points=awarded(position,system.race);if(result?.polePosition)points+=Number(system.poleBonus||0);if(result?.fastestLap&&(!system.fastestLapMaxPosition||Number(position)<=system.fastestLapMaxPosition))points+=Number(system.fastestLapBonus||0);points+=awarded(result?.sprintPosition,system.sprint||[]);return points;}
function cutoff(){return Number(document.getElementById('scenario-cutoff').value||0);}

function initialisePredictions(){predictions=new Map();scenarioData.calendar.forEach(race=>{const round=new Map();scenarioData.driverChampionship.forEach(driver=>{const result=driver.raceResults?.[String(race.round)];round.set(String(driver.driverId),result?.position?Number(result.position):null);});predictions.set(Number(race.round),round);});}
function finishCounts(driver,throughRound){const counts={};scenarioData.calendar.filter(race=>Number(race.round)<=throughRound).forEach(race=>{const result=driver.raceResults?.[String(race.round)],position=Number(race.round)<=cutoff()?result?.position:predictions.get(Number(race.round))?.get(String(driver.driverId));if(position)counts[position]=(counts[position]||0)+1;});return counts;}
function projectedPoints(driver,throughRound=scenarioData.calendar.length){const system=scenarioSystem();return scenarioData.calendar.filter(race=>Number(race.round)<=throughRound).reduce((sum,race)=>{const result=driver.raceResults?.[String(race.round)];if(Number(race.round)<=cutoff())return sum+Number(result?.points||0)+Number(result?.sprintPoints||0);return sum+futureScore(result,predictions.get(Number(race.round))?.get(String(driver.driverId)),system);},0);}
function standings(throughRound=scenarioData.calendar.length){return scenarioData.driverChampionship.map(driver=>({...driver,projectedPoints:projectedPoints(driver,throughRound),finishes:finishCounts(driver,throughRound)})).sort((a,b)=>{if(b.projectedPoints!==a.projectedPoints)return b.projectedPoints-a.projectedPoints;for(let position=1;position<=30;position++){const difference=(b.finishes[position]||0)-(a.finishes[position]||0);if(difference)return difference;}return a.name.localeCompare(b.name);});}
function clinchingRound(){const finalRound=scenarioData.calendar.length,winnerPoints=Number(scenarioSystem().race[0]||0);for(let round=cutoff();round<=finalRound;round++){const table=standings(round),remaining=finalRound-round,leader=table[0],challengerMaximum=Math.max(...table.slice(1).map(driver=>driver.projectedPoints+remaining*winnerPoints));if(leader.projectedPoints>challengerMaximum)return scenarioData.calendar.find(race=>Number(race.round)===round)||null;}return null;}

function renderScenarioGrid(){const round=Number(document.getElementById('scenario-race').value),race=scenarioData.calendar.find(item=>Number(item.round)===round),positions=predictions.get(round);if(!race)return document.getElementById('scenario-grid').innerHTML='<div class="empty-state">The selected cutoff leaves no races to predict.</div>';const drivers=[...scenarioData.driverChampionship].sort((a,b)=>(positions.get(String(a.driverId))||99)-(positions.get(String(b.driverId))||99));document.getElementById('scenario-grid').innerHTML=`<div class="scenario-race-meta"><span>Round ${race.round}</span><strong>${esc(race.officialName)}</strong><small>${esc(fmtDate(race.date))}</small></div><div class="scenario-driver-grid">${drivers.map(driver=>{const value=positions.get(String(driver.driverId)),result=driver.raceResults?.[String(round)];return `<label><span><strong>${esc(driver.name)}</strong><small>${result?.sprintPosition?`Sprint P${result.sprintPosition} retained`:result?.fastestLap?'Fastest lap bonus eligible':''}</small></span><select data-scenario-driver="${esc(driver.driverId)}"><option value="">DNF / no score</option>${drivers.map((_,index)=>`<option value="${index+1}"${value===index+1?' selected':''}>P${index+1}</option>`).join('')}</select></label>`;}).join('')}</div>`;document.querySelectorAll('[data-scenario-driver]').forEach(select=>select.addEventListener('change',()=>{const driverId=String(select.dataset.scenarioDriver),old=positions.get(driverId),next=select.value?Number(select.value):null;if(next){const other=[...positions.entries()].find(([id,position])=>id!==driverId&&position===next);if(other)positions.set(other[0],old||null);}positions.set(driverId,next);renderScenarioGrid();renderScenarioOutlook();}));}

function renderScenarioOutlook(){const table=standings(),leader=table[0],roundsLeft=scenarioData.calendar.length-cutoff(),maximum=Number(scenarioSystem().race[0]||0)*roundsLeft,clinch=clinchingRound();document.getElementById('scenario-summary').innerHTML=`<div class="scenario-leader"><span>Projected champion</span><strong>${esc(leader.name)}</strong><small>${fmtNumber(leader.projectedPoints)} points</small></div><div class="scenario-clinch"><span>Projected clinch</span><strong>${clinch?`Round ${clinch.round}`:'Final round'}</strong><small>${clinch?esc(clinch.officialName):'Title remains mathematically open'}</small></div>`;document.getElementById('scenario-standings').innerHTML=`<div class="scenario-standing-list">${table.map((driver,index)=>{const gap=leader.projectedPoints-driver.projectedPoints,current=projectedPoints(driver,cutoff()),currentLeader=Math.max(...scenarioData.driverChampionship.map(item=>projectedPoints(item,cutoff()))),alive=current+maximum>=currentLeader;return `<a href="/driver.html?id=${encodeURIComponent(driver.driverId)}"><b>${index+1}</b><span><strong>${esc(driver.name)}</strong><small>${index===0?'Projected leader':`-${fmtNumber(gap)} pts`} · ${alive?'title possible':'eliminated'}</small></span><em>${fmtNumber(driver.projectedPoints)}</em></a>`;}).join('')}</div>`;}

function populateScenarioRaces(){const future=scenarioData.calendar.filter(race=>Number(race.round)>cutoff()),select=document.getElementById('scenario-race');select.innerHTML=future.map(race=>`<option value="${race.round}">R${race.round} · ${esc(race.officialName)}</option>`).join('');renderScenarioGrid();renderScenarioOutlook();}
async function loadScenarioSeason(){const year=document.getElementById('scenario-season').value;if(!year)return;document.getElementById('scenario-grid').innerHTML='<div class="loading-state">Loading championship…</div>';try{scenarioData=await getJSON(`/api/seasons/${encodeURIComponent(year)}`);const select=document.getElementById('scenario-cutoff'),suggested=Math.max(0,scenarioData.calendar.length-5);select.innerHTML=`<option value="0">Before round 1</option>${scenarioData.calendar.slice(0,-1).map(race=>`<option value="${race.round}"${Number(race.round)===suggested?' selected':''}>Round ${race.round} · ${esc(race.officialName)}</option>`).join('')}`;initialisePredictions();populateScenarioRaces();}catch(error){setError('scenario-grid',error.message);}}
async function initialiseScenario(){try{const [seasons,systems]=await Promise.all([getJSON('/api/seasons'),getJSON('/api/points-systems')]);document.getElementById('scenario-season').innerHTML=seasons.map(season=>`<option value="${season.year}">${season.year}</option>`).join('');systems.forEach(saved=>{const key=`custom:${saved.id}`;SCENARIO_SYSTEMS[key]={name:saved.name,race:saved.racePoints,sprint:saved.sprintPoints,poleBonus:saved.poleBonus,fastestLapBonus:saved.fastestLapBonus,fastestLapMaxPosition:saved.fastestLapMaxPosition};document.getElementById('scenario-points').insertAdjacentHTML('beforeend',`<option value="${esc(key)}">${esc(saved.name)} · custom</option>`);});loadScenarioSeason();}catch(error){setError('scenario-grid',error.message);}}
document.getElementById('scenario-season').addEventListener('change',loadScenarioSeason);document.getElementById('scenario-cutoff').addEventListener('change',()=>{initialisePredictions();populateScenarioRaces();});document.getElementById('scenario-race').addEventListener('change',renderScenarioGrid);document.getElementById('scenario-points').addEventListener('change',renderScenarioOutlook);document.getElementById('reset-scenario').addEventListener('click',()=>{initialisePredictions();renderScenarioGrid();renderScenarioOutlook();});initialiseScenario();
